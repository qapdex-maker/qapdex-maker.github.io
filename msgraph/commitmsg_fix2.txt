Replace RapiDoc with lightweight endpoint list (fix freeze)

RapiDoc loaded the 8-41MB OpenAPI inline and blocked the main thread,
freezing the whole page (unendlich laden + freeze on reload). Removed
RapiDoc entirely (script tag + element + spec-url logic).

Reference tab now renders a searchable endpoint list from the existing
index.v1.0.json / index.beta.json (lazy, async fetch — no thread block),
with a "rohe Spec oeffnen" button that opens the raw YAML in a new tab.
Also fixed the earlier SITE['v1.0'] undefined -> 404 (key was 'v10').

Verified: no 'rapidoc' string remains in HTML/app.js; node -c passes;
index.v1.0.json fetch + flatten + search works locally (17531 endpoints,
rendered sliced to 300). Live tip matches after push.
