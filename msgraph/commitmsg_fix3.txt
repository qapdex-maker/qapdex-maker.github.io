Make Hub visibly interactive (fix "nothing happens")

- Root cause of user's "nix interaktives" impression: interactive tabs
  (Console, Reference) only loaded their 2.5MB data on click, so the
  container was empty until then; most other buttons just opened raw YAML.
- Now Console + Reference data are preloaded in the background during boot(),
  so switching tabs shows content immediately (no empty wait).
- Added a visible "live" indicator (● live, green glow) in the header once
  JS has run, so the page visibly proves it is alive.
- Node-checked: nlMap + renderCommands produce visible output per click.

Verified: app.js node -c passes; liveDot/console containers present in
deployed HTML; Console logic emits endpoint+permission+curl on input.
